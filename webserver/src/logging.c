#include "../include/logging.h"
#include <syslog.h>
#include <stdarg.h>

int check(char* str)
{
    
}

void log(char* format, ...)
{
    while (format)
    {
        if (check("%s"))
        {

        }
    }
    
}

void log_err(char* format, ...)
{

}